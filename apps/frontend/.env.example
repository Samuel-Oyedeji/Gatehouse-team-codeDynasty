# The frontend is now a client for the NestJS backend (gatehouse-backend).
# Point this at the running backend. Auth (JWT) and Nomba live in the backend.
VITE_API_URL="http://localhost:3000"

# Vercel (Production): set this in Project Settings → Environment Variables
# VITE_API_URL="https://gatehouse-backend-production.up.railway.app"
